module RapSheetParser
  grammar UpdateGrammar
    include DispositionGrammar

    rule update
      stuff date dispositions <Update>
    end

    rule dispositions
      disposition*
    end

    rule stuff
      (!dispo_header !date .)*
    end
  end
end
